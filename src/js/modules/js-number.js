export default () => {
  // const inputs = document.querySelectorAll('.js-number');

  // inputs.forEach(input => {
  //   input.addEventListener("input", function(){
  //    input.value = [...input.value.replace(/[^0-9]/g,"")].reduce((a,c,i,l)=>a+=c+((l.length-i)%3==1?" ":"")||a,"")
  //   });
  // })
}


// input.value = input.value.replace(/\D/g, '');
